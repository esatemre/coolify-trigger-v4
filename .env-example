# Example environment variables for docker-compose.yaml (Coolify auto-generates SERVICE_* variables)
# Copy this file to .env and fill in the required password values

# Passwords and secrets (these become SERVICE_PASSWORD_* in Coolify)
SERVICE_PASSWORD_SESSION=your-session-secret
SERVICE_PASSWORD_MAGIC=your-magic-link-secret
SERVICE_PASSWORD_ENCRYPTION=your-encryption-key
SERVICE_PASSWORD_MANAGEDWORKER=your-managed-worker-secret
SERVICE_PASSWORD_POSTGRES=your-postgres-password
SERVICE_PASSWORD_64_CLICKHOUSE=your-clickhouse-password-base64
SERVICE_PASSWORD_REGISTRY=your-registry-password
SERVICE_PASSWORD_MINIO=your-minio-password
SERVICE_PASSWORD_64_REGISTRY_HTTP_SECRET=your-registry-http-secret-base64